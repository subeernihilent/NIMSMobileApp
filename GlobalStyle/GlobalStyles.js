import { StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
    container:{
        flex:1,
        padding:30,
    },
    errorText: {
        color: 'crimson',
        fontWeight: '500',
        marginBottom: 2,
        marginTop: 2,
        textAlign: 'center',
      }
});